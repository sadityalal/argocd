apiVersion: v1
kind: Service
metadata:
  name: nginx-sample
  namespace: default
spec:
  type: NodePort
  selector:
    app: nginx-sample
  ports:
    - port: 80           # Cluster port
      targetPort: 80     # Container port
      nodePort: 30081    # NodePort for kind

