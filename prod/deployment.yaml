apiVersion: apps/v1
kind: Deployment
metadata:
  name: nginx-sample
  namespace: tomcat-prod
spec:
  replicas: 2
  selector:
    matchLabels:
      app: nginx-sample-prod
  template:
    metadata:
      labels:
        app: nginx-sample-prod
    spec:
      containers:
        - name: nginx
          image: nginx:stable
          ports:
            - containerPort: 80

